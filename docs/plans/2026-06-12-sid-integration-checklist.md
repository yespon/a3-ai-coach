# 锐捷 SID（CAS）对接确认清单

> 用途：在写认证代码**之前**，把所有外部依赖一次性向 SID 对接人 / 平台管理员锁死。
> 配套设计：[方案 C 服务端 session 设计](./2026-06-12-cas-sso-session-design.md)。

## ✅ 已确认（2026-06-12）

| 项 | 结论 |
|---|---|
| 会话方案 | **走 C：Postgres 服务端 session**（SLO 按强制处理） |
| 属性释放 | 员工返回**邮箱、姓名等**额外属性（详见对接文档字段表）；**供应商无邮箱**，代码须容忍缺失 |
| 回调 `service` | **`https://gangbiao-ai-coach.ruijie.com.cn/login`**（前端 `/login` 路由） |
| 环境 / 网络 | 网络已通；**只有生产 SID `https://sid.ruijie.com.cn`，无 UAT**；本地用 **hosts 把域名指向本机** 联调 |

## ⏳ 仍待对接人确认（开工前最好拿到）

- **BACK_CHANNEL 登出回调地址注册怎么填** + SID POST 的报文确切格式（用于解析原始 ST 撤销 session）→ 见 §4
- **飞书注册审批**：应用ID/名称/负责人(须锐捷员工)/登出地址 → 见 §6（排期阻塞，尽早发起）
- 测试工号若干（员工 + 供应商各一，覆盖有/无邮箱）→ 见 §5

---

## 1. 单点登出（SLO） — ✅ 已定走 C

- [x] 走方案 C，做 **BACK_CHANNEL** 单点登出（服务端可撤销 session）。
- [ ] 仅剩落地细节：登出回调报文格式见 §4。

---

## 2. 用户属性释放 — ✅ 员工已确认返回邮箱/姓名等

> 默认 `/p3/serviceValidate` 只返回 `<cas:user>` = **工号**。员工属性已确认释放；**供应商属性集不同（无邮箱）**。

- [x] 员工返回：`RJEMAIL`(邮箱)、`RJXM`(姓名) 等额外信息（详见对接文档字段表）。
- [ ] 确认并落实想用的字段的**确切 XML 标签名**（解析用）：
  - `RJEMAIL` → `users.email`
  - `RJXM` → `users.nickname`
  - `Position`(主岗编码) → **建议存，对岗标应用有业务价值**
  - `RJTX`(头像)、`HeadcountType`、`WORKINGLOCATION`、`SFLBDM` → 按需
- [ ] 确认**供应商/外部账号**返回的字段（如 `GYSMC`/`SHXYDM`），及"无邮箱"的处理（代码已设计为容忍 `None`）。

---

## 3. `service` 回调 URL — ✅ 已确认

- [x] 回调 `service` = **`https://gangbiao-ai-coach.ruijie.com.cn/login`**（前端 `/login` 路由，SID 带 `?ticket=` 回此页，前端再调后端 `cas/exchange`）。
- [x] 该串写死为配置 `sid_service_url`，`/login` 跳转与 `/p3/serviceValidate` 校验**逐字符共用同一串**。
- [ ] 向对接人复核：SID 侧对 `service` 是精确匹配还是前缀匹配？是否允许带额外查询参数？

---

## 4. 登出相关地址（仅 SLO 强制时需要）

- [ ] 应用主动登出后，跳转回 SID 的 **logout URL**：`https://sid(-uat).ruijie.com.cn/logout`（确认确切路径与是否带 `service` 参数）。
- [ ] **BACK_CHANNEL 登出回调地址**在注册时怎么填？SID 会向该地址 POST 什么（确认是 SAML `LogoutRequest`，含原始 ST 的 `SessionIndex`）？
- [ ] 登出回调的报文格式与字段（用于反查并撤销 session）。
- [ ] 确认要求：退出 / 会话过期后**必须回到 SID 登录页**，禁止回应用自带登录页（与现有本地登录页冲突，需下线）。

---

## 5. 网络与联调 — ✅ 网络已通；无 UAT，hosts 联调

- [x] 出网到 `https://sid.ruijie.com.cn` **已放通**。
- [x] **无独立 UAT**：只有生产 SID。本地联调通过 **hosts 把 `gangbiao-ai-coach.ruijie.com.cn` 指向开发机**，本机起 HTTPS（自签证书），直连生产 SID 完成回跳。详见设计文档 §12。
- [ ] 提供 **2-3 个测试工号 + 密码**（覆盖：普通员工、有完整属性的账号、**供应商账号(无邮箱)**、未注册账号）。
- [x] 票据：ST **一次性、生产仅 10 秒** → 校验同步立即、`httpx` 超时 ≤5s、失败不重试。
- [x] `SOURCEID_TGC` 有效期 **8 小时** → 应用 session 绝对过期上限对齐。

> 联调注意：直连生产 SID，测试登录会产生真实 SID 会话；登出测试记得清 `SOURCEID_TGC`。

---

## 6. 应用注册（飞书审批流程，外部排期依赖）

> 注册走**飞书审批**，提前发起以免阻塞联调。需提交：

- [ ] 应用 ID / 应用名称
- [ ] 应用描述
- [ ] **负责人（必须是锐捷员工）**— 确认谁挂名
- [ ] 认证协议：**CAS**（官方推荐，优先于 OAuth2.0）
- [ ] 应用地址 / 回调 URL（同第 3 项）
- [ ] 登出回调地址（同第 4 项，若需 BACK_CHANNEL）
- [ ] 预计审批时长？未注册时访问报"应用未对接认证服务"——确认注册生效前无法联调。

---

## 7. 其它需确认

- [ ] 协议选 CAS 还是 OAuth2.0？（文档官方推荐 CAS，本方案按 CAS 设计）
- [ ] 是否支持/需要 CAS Proxy（代理票据 PGT/PT）？本应用为单体，预计**不需要**，确认即可。
- [ ] SID 侧对单用户并发会话有无限制？（影响多设备登录策略）
- [ ] 生产与 UAT 的属性释放配置是否需**分别申请**？
- [ ] 异常返回报文样例：校验失败 / ticket 过期 / 未注册 各自的 XML 或 HTTP 表现（用于错误处理）。

---

## 决策路径（已大部分回填）

```
SLO ───────────> ✅ 走方案 C(服务端 session),做 §4 BACK_CHANNEL SLO 端点
属性 ──────────> ✅ 员工有邮箱/姓名;供应商无邮箱 → users 列可空,代码容忍 None
service ───────> ✅ 写死 sid_service_url = https://gangbiao-ai-coach.ruijie.com.cn/login
网络/环境 ─────> ✅ 已通;无 UAT → hosts 联调(设计文档 §12)
———— 仍阻塞 ————
§4 登出回调报文格式 ─> 实现 /cas/slo 解析 ST 需要(可先按标准 SAML LogoutRequest 假设)
§6 飞书注册审批 ────> 排期阻塞,尽早发起(应用ID/负责人/登出地址)
§5 测试工号 ───────> 联调时需要(员工+供应商各一)
```

> **开工状态：核心输入(方案/属性/service/网络)已齐，可立即按设计文档 §11 的 1→8 步开始。** §4 报文格式与 §6 注册可并行推进，不阻塞前 4 步编码。

