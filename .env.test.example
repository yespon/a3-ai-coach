# 测试环境数据库连接（含密码，请勿提交真实值）
# 与 docker-compose.test.yml 配合使用：
#   docker compose -f docker-compose.yml -f docker-compose.test.yml up -d --build
DATABASE_URL=postgresql+asyncpg://postgres:<PASSWORD>@<HOST>:<PORT>/postgres
